# Base URL for the Blysk Admin API (blysk-node backend).
# The backend must be running on this host/port (see API docs — default: port 4000).
VITE_API_BASE_URL=https://api.blyskjewels.com

# Credentials used to authenticate against the auth API automatically on load.
VITE_API_AUTH_EMAIL=admin@gmail.com
VITE_API_AUTH_PASSWORD=Rana@123
